{
  "speed": [
    { "if": "road_class == MOTORWAY", "multiply_by": 0.0 },
    { "if": "road_class == TRUNK", "multiply_by": 0.0 },
    { "if": "road_class == PRIMARY && max_speed > 45", "multiply_by": 0.1 },
    { "if": "surface == GRAVEL && road_class == TRACK", "multiply_by": 1.4 },
    { "if": "surface == GRAVEL", "multiply_by": 1.3 },
    { "if": "surface == DIRT && road_class == TRACK", "multiply_by": 1.3 },
    { "if": "surface == DIRT", "multiply_by": 1.2 },
    { "if": "surface == PAVED && road_class == RESIDENTIAL", "multiply_by": 1.0 },
    { "if": "surface == PAVED", "multiply_by": 0.9 },
    { "if": "road_class == PATH", "multiply_by": 0.8 },
    { "if": "mtb_rating >= 2", "multiply_by": 0.3 }
  ],
  "priority": [
    { "if": "road_class == MOTORWAY", "multiply_by": 0.0 },
    { "if": "road_class == TRUNK", "multiply_by": 0.0 },
    { "if": "road_class == PRIMARY && max_speed > 45", "multiply_by": 0.1 },
    { "if": "surface == GRAVEL && road_class == TRACK", "multiply_by": 2.5 },
    { "if": "surface == GRAVEL", "multiply_by": 2.0 },
    { "if": "surface == DIRT && road_class == TRACK", "multiply_by": 2.2 },
    { "if": "surface == DIRT", "multiply_by": 1.8 },
    { "if": "surface == PAVED && road_class == RESIDENTIAL", "multiply_by": 1.0 },
    { "if": "surface == PAVED", "multiply_by": 0.8 },
    { "if": "road_class == PATH", "multiply_by": 0.8 },
    { "if": "mtb_rating >= 2", "multiply_by": 0.3 }
  ]
}
