{
  "speed": [
    { "if": "surface == PAVED", "multiply_by": 1.2 },
    { "if": "surface == GRAVEL", "multiply_by": 0.2 },
    { "if": "surface == DIRT", "multiply_by": 0.1 },
    { "if": "road_class == TRACK", "multiply_by": 0.1 },
    { "if": "road_class == PATH", "multiply_by": 0.05 },
    { "if": "mtb_rating >= 1", "multiply_by": 0.01 }
  ],
  "priority": [
    { "if": "surface == PAVED", "multiply_by": 2.0 },
    { "if": "surface == GRAVEL", "multiply_by": 0.2 },
    { "if": "surface == DIRT", "multiply_by": 0.1 },
    { "if": "road_class == TRACK", "multiply_by": 0.1 },
    { "if": "road_class == PATH", "multiply_by": 0.05 },
    { "if": "mtb_rating >= 1", "multiply_by": 0.01 }
  ]
} 